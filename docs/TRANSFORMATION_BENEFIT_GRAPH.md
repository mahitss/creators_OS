# Transformation Benefit Graph & Cascade Modeling

`TransformationBenefitGraph` models benefit dependencies, detecting double-counted benefits across programs and mapping benefit cascade paths (`Transformation A + B → Capability → Benefit → Strategic Outcome`).
