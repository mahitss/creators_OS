# Hierarchical Cost Attribution & Allocation

Attributes spend across 10 organizational dimensions:
`provider -> model -> agent -> mission -> workspace -> organization`.

## Shared Infrastructure Allocation
Applies transparent allocation methods (usage-based, equal, or configured ratio) with explicit audit trails.
