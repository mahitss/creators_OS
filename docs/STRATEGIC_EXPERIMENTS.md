# Strategic Experiments & Learning Loops

`StrategicExperiment` tests uncertain strategic assumptions cheaply before making large capital commitments.

`StrategicExperimentOutcome` updates assumption confidence and thesis validity (`supports`, `weakens`, `invalidates`, `inconclusive`).
