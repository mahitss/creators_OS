# Control Loops & Operating Modes

`ControlLoop` models governed control cycles targeting missions, KPIs, capacity, cost, infrastructure, and workflows.

Supports 4 modes (`monitor_only` [default], `recommendation`, `approval_gated`, `policy_authorized`). Tracks status (`draft`, `active`, `paused`, `degraded`, `suspended`, `completed`, `retired`).
