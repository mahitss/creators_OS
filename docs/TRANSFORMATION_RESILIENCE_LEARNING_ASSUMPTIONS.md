# Assumptions & Downstream Failure Impact

## Assumption Failure Modeling
Tracks assumption validity (`TransformationResilienceLearningAssumption`) and models downstream impact when assumptions prove invalid (`TransformationResilienceLearningAssumptionFailure`).
