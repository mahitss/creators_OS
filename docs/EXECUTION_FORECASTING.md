# Predictive Execution Forecasting

`ExecutionForecast` forecasts completion dates, costs, and benefit realizations with explicit confidence bounds (`lower_bound`, `upper_bound`, `confidence_pct`).

Forecasts are clearly marked as estimates and never reported as realized facts.
