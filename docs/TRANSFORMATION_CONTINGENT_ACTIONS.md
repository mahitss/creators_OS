# Contingent Actions & Foresight Triggers

`TransformationContingentAction`, `TransformationForesightThreshold`, and `TransformationForesightTrigger` manage trigger-based preparation options.

When thresholds are breached, triggers surface review recommendations to human leadership. They strictly do NOT automatically execute strategic changes.
