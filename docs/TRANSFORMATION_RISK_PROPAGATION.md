# Risk & Decision Propagation

`TransformationRiskPropagation` and `TransformationDecisionPropagation` map how risks and governance decisions propagate through downstream transformation programs across `direct`, `indirect`, `conditional`, and `downstream` impact modes.
