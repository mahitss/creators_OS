# Transformation Dependency & Multi-Hop Propagation

Multi-hop path reasoning algorithms traverse paths (e.g. Transformation → Capability → Process → Dependency → Other Transformation), calculating downstream exposure when shared dependencies fail.

Exposes clear paths, evidence links, and confidence ratings without raw chain-of-thought traces.
