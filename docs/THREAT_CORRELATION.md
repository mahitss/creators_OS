# Associative Signal Correlation & Pattern Recognition

`ThreatCorrelation` connects signals by entity, dependency, time window, shared source, or shared outcome. Uses associative language ("associated with") unless explicit causal evidence exists.

`ThreatPattern` identifies patterns across 6 types (`cluster`, `sequence`, `trend`, `cascade`, `recurrence`, `co_occurrence`).
