# Return-to-Normal Execution Plans

`TransformationReturnToNormalPlan` structures the verified transition back to baseline transformation execution (`draft` -> `approved` -> `active` -> `verified` -> `closed`) once all recovery objectives are satisfied.
