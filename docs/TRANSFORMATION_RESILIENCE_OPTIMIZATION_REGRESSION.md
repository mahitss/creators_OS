# Recommendation Regression & Optimization Drift

## Regression & Drift Detection
Detects when a previously preferred candidate becomes inferior due to changing conditions (`TransformationResilienceOptimizationRegression`) or parameter drift (`TransformationResilienceOptimizationDrift`).
