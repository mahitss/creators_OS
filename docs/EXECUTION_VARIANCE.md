# Execution Variance Intelligence

`ExecutionVariance` tracks multi-dimensional variance across 7 types: `scope`, `schedule`, `cost`, `capacity`, `dependency`, `quality`, `benefit`.

Compares baseline vs actual vs forecast to highlight severity (`low`, `medium`, `high`, `critical`) backed by measurable evidence.
