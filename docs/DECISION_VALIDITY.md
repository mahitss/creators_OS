# Decision Validity Assessments & Reassessments

`DecisionValidityAssessment` continuously tracks whether approved decisions remain `valid`, `uncertain`, `degraded`, or `invalid`.

`DecisionReassessment` triggers review when material assumptions, forecasts, risks, or constraints change. Vapor never silently replaces approved decisions.
