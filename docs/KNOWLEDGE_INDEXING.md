# Knowledge Indexing & Chunking Strategy

## Deterministic Chunking
Supports source-aware chunking with token estimates, section headers, and bounded overlap. Documents maintain historical versions (`KnowledgeDocumentVersion`) with parser and chunking version tags.
