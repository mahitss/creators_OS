# Transformation Pattern Recognition

`TransformationPattern` detects recurring failure and friction patterns (`dependency_failure`, `capacity_bottleneck`, `benefit_lag`, `decision_delay`, `adoption_friction`, `scope_drift`) with supporting observation evidence.
