# Governed Portfolio Rebalancing

`TransformationPortfolioRebalance` formulates evidence-backed rebalancing recommendations due to capacity shifts, scenario stress, benefit variance, or strategy updates.

Material rebalance actions require explicit human leadership authorization (`Decision`, `Approval`, `PolicyEngine`) before execution via `Execution Governance` and `ActionGateway`.
