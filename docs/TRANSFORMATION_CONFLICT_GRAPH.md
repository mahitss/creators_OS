# Transformation Conflict Graph

`TransformationConflictGraph` flags target operating model, technology, process, decision rights, capacity, and timeline conflicts between concurrent initiatives across 4 severity levels (`low`, `medium`, `high`, `critical`).
