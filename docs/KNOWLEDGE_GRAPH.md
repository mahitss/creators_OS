# Lightweight Knowledge Graph Engine

## Entity & Relationship Ontology
Manages lightweight entity relationships (`owns`, `created_by`, `belongs_to`, `mentions`, `related_to`, `depends_on`, `member_of`, `assigned_to`, `part_of`). Relationships inherit source document permissions to prevent unauthorized graph traversal.
