# Cross-Transformation Impact Intelligence

`CrossTransformationImpact` and `TransformationImpactMap` track multi-dimensional downstream impacts across strategy, capabilities, processes, organizational units, systems, and dependencies.

Supports impact classifications across `positive`, `negative`, `blocking`, `enabling`, `conditional`, and `uncertain`.
