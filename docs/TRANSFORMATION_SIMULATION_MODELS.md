# Simulation Models & Parameters

`TransformationSimulationModel` defines assumptions, parameters, and limitations across 10 model types (`dependency_propagation`, `capacity`, `timeline`, `risk`, `benefit`, `governance`, `scenario`, `portfolio`, `operating_model`, `combined`).
