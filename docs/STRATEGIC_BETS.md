# Strategic Bets & Portfolio Exposure Analysis

`StrategicBet` tracks committed investments, thesis, expected outcomes, supported scenarios, and review dates across 7 statuses (`proposed`, `approved`, `active`, `under_review`, `successful`, `failed`, `retired`).

`StrategicExposure` detects excessive reliance on a single assumption or scenario.
