# Governed Transformation Change Requests & Simulation

`TransformationChangeRequest` formulates scope, sequence, timing, capacity, dependency, design, risk mitigation, and wave transition change proposals.

Simulates impact against current and alternative plans, requiring explicit human leadership approval (`Decision`, `Approval`, `PolicyEngine`) before execution via `Execution Governance` and `ActionGateway`.
