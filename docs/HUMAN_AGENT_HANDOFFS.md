# Context-Preserving Human-Agent Handoffs

`WorkHandoff` manages bidirectional context transfers between humans and agents (`Human -> Agent`, `Agent -> Human`, `Human -> Human`, `Agent -> Agent`).

## DLP & Context Filtering
Transfers only authorized, task-relevant context. Enforces DLP pattern masking and data classification limits prior to handoff delivery.
