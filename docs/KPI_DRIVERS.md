# Associated Drivers & Correlation Safeguards

`KPIDriver` identifies operational factors associated with KPI movements.

Strictly uses `associated_type = correlated` or `inferred` to prevent claiming unevidenced causality.
