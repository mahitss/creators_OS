# Structured Agent Artifact Exchange

## Schema-Validated Artifacts
Agents communicate primarily through structured, validated artifacts (`AgentArtifact`) rather than raw free-form chat. Artifact types include `research_report`, `dataset`, `analysis`, `code_patch`, `document`, `recommendation`, `validation_report`, and `decision`.
