# Capability, Skill & Automation Gap Engine

`CapabilityGap` detects missing, restricted, or under-capacity capabilities required by active missions.

Classifications: `missing`, `under_capacity`, `restricted`, `unavailable`, `unknown`.

Advises on automation opportunities and human review needs without inferring sensitive personal worker attributes.
