# Enterprise Portfolio Management & Tracking

`Portfolio` manages portfolios (`draft`, `active`, `under_review`, `paused`, `completed`, `archived`) with versioned snapshots (`PortfolioVersion`).

Provides high-level visibility across active programs, initiatives, resource allocations, and health scores.
