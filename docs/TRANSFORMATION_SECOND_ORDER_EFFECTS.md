# Second & Third-Order Propagation Effects

`TransformationSecondOrderEffect` traces multi-step propagation paths (`Technology Change → Capability Gap → Transformation Delay → Benefit Lag`).

Exposes clear multi-order paths with evidence and confidence scores.
