# Business Continuity Plans & Versioning

`ContinuityPlan` defines immutable, versioned plans with triggers, actions, owners, dependencies, communications, and rollback strategies.

Tracks plan statuses (`draft`, `active`, `stale`, `testing`, `validated`, `retired`). Stale plans trigger review alerts.
