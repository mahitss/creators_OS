# Service Accounts & Machine Identities

## Scoped Machine Credentials
Service accounts use hashed, rotatable tokens with explicit scopes (`organization.read`, `workflow.run`). Raw tokens are never logged or stored plaintext.
