# Capability Versioning & Immutability

Published `CapabilityVersion` snapshots are immutable.

## Version Resolution
Supports exact version pinning for critical workflows and compatible version ranges for general executions.
