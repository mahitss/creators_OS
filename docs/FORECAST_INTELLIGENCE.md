# Forecast Intelligence Engine

`Forecast` generates multi-horizon predictions (`short_term`, `medium_term`, `long_term`, `custom`) across 10 entity types (`kpi`, `benefit`, `initiative`, `milestone`, `portfolio`, `capacity`, `cost`, `risk`, `outcome`, `mission`).

`ForecastPoint` tracks timestamped values with lower/upper confidence bounds.
