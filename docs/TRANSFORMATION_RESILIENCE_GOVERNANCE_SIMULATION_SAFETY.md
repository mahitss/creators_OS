# Simulation Governance & Sandbox Isolation Safety

## Isolation Verification
Enforces sandbox isolation (`CTRL_SIMULATION_ISOLATION`), source snapshot immutability, scenario reproducibility, and zero production mutation guardrails (`CTRL_PRODUCTION_MUTATION_PREVENTION`).
