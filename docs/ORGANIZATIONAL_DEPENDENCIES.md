# Organizational Dependency & Critical Path Management

`OperatingDependency` tracks edges across missions, workflows, integrations, and decisions.

## Critical Path & Health Tracking
Monitors health (`healthy`, `degraded`, `blocked`, `unknown`) and marks critical-path edges (`is_critical_path`) that directly impact deadlines and business outcomes.
