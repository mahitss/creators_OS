# Skill Versioning & Immutability

Published `AgentSkillVersion` snapshots are strictly immutable.

## Version Controls
- Modifications create new incremental version records (`v1`, `v2`, `v3`).
- Historical executions reference exact skill version IDs, preserving auditability and reproducibility.
