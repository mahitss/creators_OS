# Counterfactual Simulation

`TransformationDecisionCounterfactual` simulates alternative decision paths ("What if we had chosen Option B?") using evidence-backed simulation models, explicitly preserving uncertainty estimates.
