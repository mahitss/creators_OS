# Capability Registry & Schema Validation

## Capability Abstraction
Capabilities define supported actions (`gmail.search`, `gmail.send`, `drive.search`, `drive.create`, `calendar.list`, `calendar.create`). Inputs and outputs are strictly validated against JSON schemas before execution.
