# Recovery Paths & Option Selection

`TransformationRecoveryPath` and `TransformationRecoveryOption` represent sequences of recovery choices (`restore`, `reroute`, `substitute`, `degrade_gracefully`, `pause`, `resequence`, `rollback`, `isolate`, `accelerate_recovery`) with prerequisite mapping and secondary impact evaluation.
