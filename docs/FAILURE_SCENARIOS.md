# Failure Scenarios & Cascading Analysis

`FailureScenario` models 11 failure types (`service_failure`, `vendor_outage`, `agent_failure`, etc.) reusing Simulation Lab.

Traces multi-hop cascading failures across downstream capabilities and dependencies.
