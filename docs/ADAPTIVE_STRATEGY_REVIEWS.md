# Adaptive Strategy Reviews & Learning Outcomes

`AdaptiveStrategyReview` logs periodic reviews across 5 cadences (`monthly`, `quarterly`, `semiannual`, `annual`, `event_driven`).

`StrategyLearning` and `DecisionRegretAnalysis` evaluate expected vs actual outcomes, recording lessons without mislabeling counterfactuals.
