# Execution Drift Signals & Velocity Tracking

`ExecutionDriftSignal` detects 7 drift types (`priority`, `scope`, `schedule`, `dependency`, `resource`, `benefit`, `alignment`) across 4 severities (`low`, `medium`, `high`, `critical`).

Execution velocity tracks planned vs actual progress, cycle time, throughput, and completion rates.
