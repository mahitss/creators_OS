# Strategic Reviews & Immutable Versioning

`StrategicReview` manages periodic reviews (`weekly`, `monthly`, `quarterly`, `custom`) evaluating progress, assumptions, risks, and resource usage.

Plan changes generate immutable snapshots (`StrategicPlanVersion`) preserving historical planning context.
