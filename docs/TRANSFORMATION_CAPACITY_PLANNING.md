# Transformation Capacity Planning & Capital Constraints

`TransformationCapacityPlan`, `TransformationCapitalConstraint`, and `TransformationCapacityConstraint` evaluate aggregate capability, team/unit, system, vendor, and budget capacity.

Detects capacity conflicts when multiple transformation candidates compete for shared resources.
