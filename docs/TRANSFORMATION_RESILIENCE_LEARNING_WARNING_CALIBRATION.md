# Warning Calibration & Multi-Metric Quality

## Multi-Metric Quality Evaluation
Tracks warning performance explicitly via precision %, recall %, average lead time (hours), false positive rate, false negative rate, and confidence calibration score (`TransformationResilienceLearningWarningQuality`). Never collapses warning accuracy into a single opaque number.
