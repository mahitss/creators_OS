# Recovery Procedures & ActionGateway Execution

`RecoveryProcedure` specifies step-by-step actions, preconditions, expected durations, and verification criteria.

Execution routes through `Universal Action Gateway` and `PolicyEngine`. Post-recovery state is explicitly verified (`RecoveryOutcome`).
