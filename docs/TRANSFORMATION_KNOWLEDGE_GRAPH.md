# Cross-Transformation Knowledge Graph 2.0

`TransformationGraphNode`, `TransformationGraphEdge`, and `TransformationGraphProvenance` map relationships across 18 entity types and 19 relationship types (`supports` to `exposes`).

Every edge retains explicit provenance classification (`observed`, `declared`, `derived`, `inferred`) and confidence metrics.
