# Feasible Options & Pareto Frontier Analysis

`OptimizationOption` tracks decision variables, constraint satisfaction, expected outcome, cost, risk, and confidence.

`OptimizationTradeoff` evaluates multi-objective trade-offs and identifies non-dominated Pareto frontier alternatives without collapsing options into one arbitrary winner.
