# Conflict Cascades & Cluster Analysis

## Cascade Intelligence
Detects when resolving Conflict A triggers downstream Conflict B.
Groups related conflicts into clusters (`shared_dependency`, `shared_resource`, `shared_evidence`, `shared_deadline`).
