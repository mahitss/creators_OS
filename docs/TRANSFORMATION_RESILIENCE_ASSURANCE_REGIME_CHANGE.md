# Regime Change Detection

## Status Tracking
Detects when historical patterns may no longer be reliable (`suspected`, `probable`, `confirmed`, `rejected`). Never automatically discards historical evidence.
