# Data Loss Prevention (DLP) Engine

## Deterministic Policy Actions
AI classifies and detects patterns. Deterministic PolicyEngine controls decide final actions: `ALLOW`, `REDACT`, `BLOCK`, `REQUIRE_APPROVAL`, `QUARANTINE`, `BLOCK_EXTERNAL_TRANSFER`.
