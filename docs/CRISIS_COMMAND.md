# Incident Command Structure & Roles

`CrisisCommand` tracks 7 explicit command roles (`incident_commander`, `operations_lead`, `technical_lead`, `security_lead`, `communications_lead`, `business_lead`, `recovery_lead`).

Command assignments do not automatically grant permissions; operations remain governed by Identity & PolicyEngine. Tracks commander handoffs (`CrisisCommandAssignment`) with explicit acknowledgment.
