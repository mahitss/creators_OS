# Data Quality & Source Reconciliation

`KPIDataQualityIssue` flags missing, stale, or invalid measurements.

`KPIReconciliation` detects multi-source conflicts (`source_a` vs `source_b`) and flags variance without silently picking one source without configured authority.
