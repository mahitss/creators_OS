# Decision Regret Analysis & Counterfactuals

`DecisionRegretAnalysis` evaluates selected options against alternative options and actual outcomes.

Counterfactuals are explicitly labeled as `simulated`, `estimated`, or `unknown` to prevent unevidenced certainty claims.
