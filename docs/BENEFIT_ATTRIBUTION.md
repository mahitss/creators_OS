# Benefit Attribution & Anti-Double-Counting

`BenefitAttribution` classifies benefit linkage as `direct`, `correlated`, or `inferred`.

Strictly prevents double-counting the same benefit across multiple initiatives or programs. Causality is never claimed without explicit causal evidence.
