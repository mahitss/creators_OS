# Chaos Engineering & Abort Triggers

Controls fault injection experiments (`ResilienceExperiment`) across sandbox and production environments.

## Abort Triggers
Experiments define explicit blast radius boundaries and automated abort conditions (e.g., error rate > 5%, latency > 3000ms).
