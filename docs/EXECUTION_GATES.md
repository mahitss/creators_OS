# Quality & Governance Gates

`ExecutionGate` enforces configured governance gates across 6 dimensions: `approval`, `security`, `quality`, `budget`, `compliance`, `business`.

Gate statuses (`pending`, `passed`, `failed`, `waived`). Waivers require an authorized actor, documented reason, timestamp, and audit trail (`No Silent Waivers`).
