# Decision Rights Matrix (RACI Governance)

`DecisionRight` and `DecisionRightsMatrix` define decision authority rules across 5 roles (`decides`, `approves`, `recommends`, `consulted`, `informed`).

Leverages existing Identity & PolicyEngine to prevent authorization logic duplication.
