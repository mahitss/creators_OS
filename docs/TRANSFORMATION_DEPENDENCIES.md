# Transformation Dependency Graph & Critical Path

`TransformationDependency` maps prerequisite and dependent structural changes across transformation programs.

Leverages Operating Graph to trace critical paths, detect circular dependencies, and highlight blocked transformation steps.
