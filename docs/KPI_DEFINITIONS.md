# KPI Formula Definitions & Lineage

`KPI` requires explicit formula, unit, population, measurement frequency, and source.

Every definition change creates a new `KPIVersion` snapshot while preserving historical definitions as immutable.
