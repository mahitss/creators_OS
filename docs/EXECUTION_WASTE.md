# Execution Waste & Quality Rework

`ExecutionWasteSignal` detects 6 waste types (`duplicate_work`, `low_value_work`, `blocked_work`, `rework`, `unused_output`, `benefit_failure`).

`ExecutionQualityAssessment` tracks rework, failure, rollback, and defect rates.
