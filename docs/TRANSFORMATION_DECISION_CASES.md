# Transformation Decision Cases & Questions

`TransformationDecisionCase` and `TransformationDecisionQuestion` define clear decision cases across 16 decision types (`start` to `change_target_state`) and 11 statuses (`draft` to `reopened`), pairing every case with a precise core question.
