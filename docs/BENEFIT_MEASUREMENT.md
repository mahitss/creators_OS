# Benefit Measurement & Evidence Verification

`BenefitMeasurement` records baseline, current, target, and variance data over defined measurement periods.

`BenefitEvidence` verifies data sources (`unverified`, `verified`, `rejected`, `stale`). Benefits without available measurements remain `status = unknown` without fabricated estimates.
