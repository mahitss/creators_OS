# Continuous Transformation Learning & Drift Detection

`TransformationDriftSignal` detects divergence between approved target models, implemented states, and empirically observed operating behavior.

`TransformationLesson` records hypothesis, decision, change, actual outcome, and lesson learned to feed continuous operating model redesign.
