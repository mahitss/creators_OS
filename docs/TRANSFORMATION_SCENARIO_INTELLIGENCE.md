# Transformation Scenario & Future State Intelligence

`TransformationFutureState` and `TransformationScenarioImpact` model baseline, alternative, stress, disruptive, and opportunity future states.

Expresses impact across probability ranges (`low`, `expected`, `high`) rather than false precision single numbers.
