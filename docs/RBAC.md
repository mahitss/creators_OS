# Role-Based Access Control (RBAC)

## Server-Side Enforcement
Frontend visibility is UX-only. The backend independently enforces permissions on every endpoint using `PolicyEngine` and user token claims.
