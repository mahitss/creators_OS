# Transformation Dependency Graph & Cycle Detection

`TransformationDependencyGraph` traces predecessor/successor relationships across 9 dependency types (`technical`, `capability`, `organizational`, `regulatory`, `financial`, `capacity`, `data`, `process`, `decision`).

Automatically identifies critical paths, parallelization opportunities, and rejects circular dependency cycles.
