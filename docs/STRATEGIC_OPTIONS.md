# Strategic Options & Reversibility Classification

`StrategicOption` evaluates strategic choices across 9 types (`invest`, `wait`, `experiment`, `hedge`, `partner`, `diversify`, `exit`, `build`, `acquire`) and classifies reversibility (`highly_reversible`, `reversible`, `partially_reversible`, `irreversible`).

Reversible options are prioritized to maintain maximum flexibility during high uncertainty.
