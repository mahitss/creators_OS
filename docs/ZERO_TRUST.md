# Zero-Trust Authorization Model

## Contextual Evaluation
Every request evaluates `identity`, `session`, `organization`, `workspace`, `resource`, `action`, and `policy` via PolicyEngine before granting access.
