# Sensitive Data & Secret Detection

## Pattern Detectors
Deterministic regex & heuristic detectors identify API keys, private keys, JWT tokens, credit card numbers, emails, and phone numbers. High-confidence secrets trigger redaction or blocking and are NEVER logged.
