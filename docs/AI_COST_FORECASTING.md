# Cost Forecasting & Spend Anomaly Detection

Generates period projections (`CostForecast`) with upper and lower confidence bounds based on historical consumption trends.

## Anomaly Signals
Detects sudden usage spikes (`SpendAnomaly`) and identifies primary cost drivers (`CostDriver`).
