# Decision-Rights Architecture & Authority Matrix

`TransformationDecisionRight` and `TransformationDecisionRightMatrix` define decision-rights mapping across authority levels, required evidence, approval requirements, escalation requirements, and delegation allowances while flagging authority conflicts (`TransformationDecisionRightConflict`).
