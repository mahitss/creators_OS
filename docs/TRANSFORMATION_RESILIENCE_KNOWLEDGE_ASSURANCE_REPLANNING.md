# Continuous Replanning & Option Baseline Comparison

## Replan Options
Generates replan options across: `continue`, `minor_adjustment`, `resequence`, `replace_action`, `add_action`, `remove_action`, `defer`, `accelerate`, `full_replan`.
Always compares alternatives against "continue current plan".
