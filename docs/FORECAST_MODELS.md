# Forecast Model Provenance & Model Drift

Tracks model metadata (`method`, `modelVersion`, `featureSetVersion`, `trainingWindow`, `generatedAt`).

`ForecastModelDrift` detects accuracy deterioration comparing baseline vs current accuracy to trigger retraining recommendations.
