# Versioned Operational Runbooks

## Controlled Runbook Execution
Runbooks define structured, policy-governed steps (`trigger_condition`, `steps`, `verification`, `rollback`). Runbooks do not contain arbitrary code.
