# Operating Process Intelligence

`OperatingProcess` and `ProcessStep` map enterprise processes, inputs, outputs, systems, dependencies, and owners.

`ProcessBottleneck` flags 7 bottleneck types (`capacity`, `decision`, `dependency`, `technology`, `handoff`, `approval`, `data`).
