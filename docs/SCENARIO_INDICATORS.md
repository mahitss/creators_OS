# Leading Scenario Indicators & Review Triggers

`ScenarioIndicator` monitors movement toward a specific future scenario by tracking baseline value, threshold value, current value, and movement direction.

`StrategicTrigger` generates recommendations (`review`, `experiment`, `re_forecast`, `re_optimize`, `re_plan`) without automatically altering strategic commitments.
