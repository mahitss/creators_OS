# Recovery Gates & Verification Checkpoints

`TransformationRecoveryGate` and `TransformationRecoveryCheckpoint` enforce strict condition criteria (e.g. stabilization complete, dependency restored, capacity sufficient) before recovery execution can advance to subsequent phases.
