# Outcome Comparison & Prediction Error Categorization

## Closed-Loop Comparison
Compares expectations vs actual observed values (`TransformationResilienceLearningOutcomeComparison`), measuring variance direction (`better_than_expected, worse_than_expected, on_target, late, early, unknown`) and categorizing prediction error types (`severity_error, timing_error, probability_error, scope_error, propagation_error, recovery_error`).
