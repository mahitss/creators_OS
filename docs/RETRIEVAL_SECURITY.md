# Retrieval Security & Grounded Citations

## Grounded Citation Rules
Every retrieved chunk included in AI context contains citation metadata (`document_id`, `title`, `source_name`, `source_url`, `classification`). AI responses reference actual source IDs. If supporting evidence is lacking, responses explicitly report insufficient evidence instead of fabricating citations.
