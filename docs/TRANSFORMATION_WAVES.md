# Transformation Waves & Exit Criteria Governance

`TransformationWave` structures execution into waves across 5 wave types (`foundation`, `pilot`, `scale`, `optimization`, `stabilization`).

Enforces explicit exit criteria (capability ready, benefit proven, risk acceptable, adoption sufficient) before wave completion.
