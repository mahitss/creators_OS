# Decision Pattern Detection

`TransformationDecisionPattern` automatically detects recurring decision types, assumption failure modes, forecast errors, dependency bottlenecks, and successful sequences across historical cases, requiring sample sizes and explicit limitations.
