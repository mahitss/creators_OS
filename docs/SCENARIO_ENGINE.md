# What-If Scenario Engine & Sandbox Simulation

## Sandbox Simulation Safety
Runs deterministic what-if simulations (`+30% workload growth`, `provider failure`, `agent concurrency doubling`, `budget cut`) in isolated sandbox mode without mutating production systems, providers, workflows, or budgets. Displays baseline, scenario output, delta, assumptions, and uncertainty.
