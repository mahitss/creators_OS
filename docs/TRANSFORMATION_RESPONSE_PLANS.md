# Orchestrated Response Plans & Checkpoints

`TransformationResponsePlan` and `TransformationResponseCheckpoint` structure the lifecycle of intervention responses (`draft` -> `analysis` -> `awaiting_decision` -> `awaiting_approval` -> `approved` -> `executing` -> `verified` -> `closed`) with checkpoint monitoring.
