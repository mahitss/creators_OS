# Digital Twin State Synchronization & Rebuilding

## Synchronization Modes
Supports `event_driven`, `scheduled_reconciliation`, and `manual_rebuild`. Tracks telemetry lag seconds, error counts, and rebuild status.
