import { TransformationGovernanceWorkspace } from '@/components/transformation-governance/TransformationGovernanceWorkspace';

export const metadata = {
  title: 'Enterprise Adaptive Governance 2.0 | Vapor OS',
  description: 'Decision Rights → Controls → Effectiveness → Friction & Gaps → Overcontrol → Load → Bottlenecks → Delegation → Change Simulation → Human Approval.',
};

export default function TransformationGovernancePage() {
  return <TransformationGovernanceWorkspace />;
}
