import { SecurityOperationsWorkspace } from "@/components/admin/SecurityOperationsWorkspace";

export const metadata = {
  title: "SecOps Operations Center | Vapor OS",
  description: "Enterprise AI Security Operations Center, Incident Response, Detection Shadow Mode & SLA Metrics"
};

export default function SecurityOperationsPage() {
  return <SecurityOperationsWorkspace />;
}
