import { CrisisIntelligenceWorkspace } from '@/components/crisis/CrisisIntelligenceWorkspace';

export const metadata = {
  title: 'Enterprise Crisis Intelligence & Coordinated Response 2.0 | Vapor OS',
  description: 'Governed crisis operating layer connecting Signal → Detection → Declaration → Impact Map → Command Structure → Response Options → Approval → Coordinated Action → Communication → Recovery Verification.',
};

export default function CrisisPage() {
  return <CrisisIntelligenceWorkspace />;
}
