import { TransformationWarRoomWorkspace } from '@/components/transformation-war-room/TransformationWarRoomWorkspace';

export const metadata = {
  title: 'Transformation War Room 2.0 | Vapor OS',
  description: 'Real State → Live Signals → Deviation Detection → Root Causes → Intervention Simulation → Human Approval → Verified Execution.',
};

export default function TransformationWarRoomPage() {
  return <TransformationWarRoomWorkspace />;
}
