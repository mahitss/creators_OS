import { TransformationDecisionsWorkspace } from '@/components/transformation-decisions/TransformationDecisionsWorkspace';

export const metadata = {
  title: 'Enterprise Transformation Decision Intelligence 3.0 | Vapor OS',
  description: 'Evidence Packs → Option Analysis → Scenario Testing → Trade-offs → Readiness → Decision Packets → Human Approval → Execution & Verification.',
};

export default function TransformationDecisionsPage() {
  return <TransformationDecisionsWorkspace />;
}
