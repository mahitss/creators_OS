import { TransformationWorkspace } from '@/components/transformation/TransformationWorkspace';

export const metadata = {
  title: 'Enterprise Operating Model Transformation 2.0 | Vapor OS',
  description: 'Current State → Observed Friction → Strategic Requirements → Design Options → Future Models → Scenario Simulation → Roadmap → Governed Execution → Adoption → Outcomes.',
};

export default function TransformationPage() {
  return <TransformationWorkspace />;
}
