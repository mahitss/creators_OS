import { AdaptiveControlWorkspace } from '@/components/control/AdaptiveControlWorkspace';

export const metadata = {
  title: 'Adaptive Decision Governance & Closed-Loop Control 2.0 | Vapor OS',
  description: 'Governed closed-loop operating cycle with real-time signals, decision validity assessments, guardrails, and post-action verification.',
};

export default function ControlPage() {
  return <AdaptiveControlWorkspace />;
}
