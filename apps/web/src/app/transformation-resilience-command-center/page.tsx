import React from 'react';
import { TransformationResilienceCommandCenterWorkspace } from '@/components/transformation-resilience-command-center/TransformationResilienceCommandCenterWorkspace';

export const metadata = {
  title: 'Vapor OS — Enterprise Transformation Resilience Command Center 2.0',
  description: 'Executive operating layer unifying continuous resilience sensing, portfolio exposure, systemic risk, evidence quality, and decision packet generation.',
};

export default function TransformationResilienceCommandCenterPage() {
  return <TransformationResilienceCommandCenterWorkspace />;
}
