import { ExecutionIntelligenceWorkspace } from '@/components/execution/ExecutionIntelligenceWorkspace';

export const metadata = {
  title: 'Enterprise Strategic Execution Intelligence 2.0 | Vapor OS',
  description: 'Strategy → Objectives → Portfolio → Programs → Initiatives → Missions → Execution → Deliverables → Outcomes → Benefits → Strategic Impact.',
};

export default function ExecutionIntelligencePage() {
  return <ExecutionIntelligenceWorkspace />;
}
