import { TransformationControlWorkspace } from '@/components/transformation-control/TransformationControlWorkspace';

export const metadata = {
  title: 'Enterprise Transformation Control Tower 2.0 | Vapor OS',
  description: 'Live Strategy → Sequence → Waves → Signals → Drift → Root Cause → Governed Change Requests → Execution Governance.',
};

export default function TransformationControlPage() {
  return <TransformationControlWorkspace />;
}
