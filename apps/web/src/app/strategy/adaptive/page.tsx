import { AdaptiveStrategyWorkspace } from '@/components/strategy/AdaptiveStrategyWorkspace';

export const metadata = {
  title: 'Enterprise Adaptive Strategy & Dynamic Portfolio Reconfiguration 2.0 | Vapor OS',
  description: 'Human-governed strategy adaptation connecting Intent → Theses → Assumptions → Live Indicators → Drift Signals → Portfolio Reconfiguration Proposals → Leadership Approval.',
};

export default function AdaptiveStrategyPage() {
  return <AdaptiveStrategyWorkspace />;
}
