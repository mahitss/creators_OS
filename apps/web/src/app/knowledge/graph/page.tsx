import React from 'react';
import { GlobalGraphWorkspace } from '@/components/knowledge/GlobalGraphWorkspace';

export const metadata = {
  title: 'Enterprise Semantic Graph — Vapor OS',
  description: 'Unified Relationship Layer & Authorization-Aware Business Context'
};

export default function GraphPage() {
  return <GlobalGraphWorkspace />;
}
