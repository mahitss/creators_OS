import React from 'react';
import { GlobalGraphWorkspace } from '@/components/knowledge/GlobalGraphWorkspace';

export const metadata = {
  title: 'AI Relationship Proposal Review — Vapor OS',
  description: 'Human Review & Verification Gate for Derived Semantic Relationships'
};

export default function ReviewPage() {
  return <GlobalGraphWorkspace />;
}
