import { ForesightIntelligenceWorkspace } from '@/components/foresight/ForesightIntelligenceWorkspace';

export const metadata = {
  title: 'Enterprise Strategic Foresight & Long-Horizon Scenario Intelligence 2.0 | Vapor OS',
  description: 'Long-horizon intelligence layer connecting Weak Signals → Trends → Drivers → Uncertainties → Assumptions → Plausible Scenarios → Strategic Options → Monitoring Triggers.',
};

export default function ForesightPage() {
  return <ForesightIntelligenceWorkspace />;
}
