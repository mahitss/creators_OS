import { OperatingModelWorkspace } from '@/components/operating/OperatingModelWorkspace';

export const metadata = {
  title: 'Enterprise Organizational Operating Intelligence 2.0 | Vapor OS',
  description: 'Strategy → Operating Model → Capabilities → Org Units → Decision Rights → Processes → Dependencies → Workflows → Execution → Outcomes.',
};

export default function OperatingModelPage() {
  return <OperatingModelWorkspace />;
}
