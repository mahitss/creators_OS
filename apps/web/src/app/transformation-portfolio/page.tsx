import { TransformationPortfolioWorkspace } from '@/components/transformation-portfolio/TransformationPortfolioWorkspace';

export const metadata = {
  title: 'Enterprise Transformation Portfolio Intelligence + Change Sequencing 2.0 | Vapor OS',
  description: 'Transformation Candidates → Dependencies → Capacity → Risk → Optionality → Change Sequencing → Wave Rollout → Governed Rebalancing.',
};

export default function TransformationPortfolioPage() {
  return <TransformationPortfolioWorkspace />;
}
