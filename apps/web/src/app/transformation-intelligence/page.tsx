import { TransformationIntelligenceWorkspace } from '@/components/transformation-intelligence/TransformationIntelligenceWorkspace';

export const metadata = {
  title: 'Enterprise Transformation Intelligence Fabric 2.0 | Vapor OS',
  description: 'Strategy → Capabilities → Transformations → Workstreams → Multi-Hop Paths → Shared Assumptions → Benefit Cascade → Pattern Reasoning.',
};

export default function TransformationIntelligencePage() {
  return <TransformationIntelligenceWorkspace />;
}
